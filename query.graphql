query {
  repository(owner:"ifyouseewendy", name:"verbose-disco" ) {
		issues(first: 50, states: CLOSED) {
      nodes {
        title
        url
        labels (first: 5) {
          nodes {
            name
          }
        }
      }
    }
  }
}
